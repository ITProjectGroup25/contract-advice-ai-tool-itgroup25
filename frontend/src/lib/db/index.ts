// Stub file - database operations should use @backend
// This file exists to prevent build errors during module resolution

export const db = null as any;
export const schema = null as any;
export const sqlClient = null as any;
